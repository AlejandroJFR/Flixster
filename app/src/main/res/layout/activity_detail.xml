<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FA3C3B3B"
    android:orientation="vertical"
    android:scrollbarSize="4dp"
    tools:context=".DetailActivity">

    <com.google.android.youtube.player.YouTubePlayerView
        android:id="@+id/player"
        android:layout_width="match_parent"
        android:layout_height="250dp" />

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-black"
        android:textColor="#BAFFFFFF"
        android:textSize="30sp"
        tools:text="It Chapter Two" />

    <RatingBar
        android:id="@+id/ratingBar"
        style="@style/Widget.AppCompat.RatingBar.Indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"
        android:numStars="10"
        android:progressTint="#1BF4D4"
        android:rating="8.5" />

    <TextView
        android:id="@+id/releaseDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="#BAFFFFFF"
        tools:text="Release Date: 12/05/2021" />

    <TextView
        android:id="@+id/adultRating"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:textColor="#BAFFFFFF"
        tools:text="Movie for adults: false" />

    <TextView
        android:id="@+id/tvOverview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-condensed"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="#BAFFFFFF"
        tools:text="27 years after overcoming the malevolent supernatural entity Pennywise, the former mebers of the Loser's Club, who have grown up and moved away from Derry , are brought back together by a devastasting phone call." />

</LinearLayout>